# Clara - Configuration principale

# LLM Settings
model: gpt-5.1
temperature: 0.7
max_tokens: 4096
language_policy: auto

# Session Settings
max_history_messages: 20

# Paths
logs_sessions_dir: logs/sessions
logs_debug_dir: logs/debug
memory_db_path: memory/clara_memory.db
